{
          "$schema": "${SCHEMA_URL}",
               "id": "${FILE_URL}",
            "title": "${TITLE}",
      "description": "${DESCRIPTION}",
       "x-createDt": "${DATE}",
       "x-createBy": "${AUTHOR}",
       "x-updateDt": "",
       "x-updateBy": ""
}
